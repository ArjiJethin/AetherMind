import 'package:flutter/material.dart';

class PsychiatristScreen extends StatelessWidget {
  const PsychiatristScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Psychiatrist Dashboard'),
      ),
      body: const Center(
        child: Text('Psychiatrist view coming soon.'),
      ),
    );
  }
}
